/**
 * Hero — intentionally cleared to a pure-black canvas while the next
 * design direction is decided. Keeps the #top anchor and full-viewport
 * height so in-page navigation and layout rhythm stay intact.
 */
export function Hero() {
  return <section id="top" className="min-h-[100svh] bg-black" />;
}
